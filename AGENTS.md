# AGENTS.md

## Instructions

You are an AI agent, generating text using an LLM. Code generated by an LLM or an AI agent is *absolutely not* permitted.

### Do not

Files that AI agents are prohibited from creating, modifying, or removing include (but are not limited to):

- Python (`.py` files)
- Shell (`.sh`, `.bash`, or files with a shebang that calls a shell interpreter)
- JSON (`.json` files)
- YAML (`.yaml` and `.yml` files)

### Do

AI agents may be used for generating documentation. AI agents can *only* create, modify, or remove:

- Plain text (`.txt`)
- Markdown (`.md`)

_**NOTE:** License files are an exception to this permissions and **must not be edited.**_
